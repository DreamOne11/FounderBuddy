# 环境配置 - Founder Buddy
# 本地开发环境配置

# 环境切换：'local' 或 'production'
NEXT_PUBLIC_API_ENV=local

# API 端点配置
# 本地后端API地址（默认端口8080）
VALUE_CANVAS_API_URL_LOCAL=http://localhost:8080

# 生产环境API地址（如果需要）
VALUE_CANVAS_API_URL_PRODUCTION=https://your-production-api.com

# API Token (可选，如果后端设置了AUTH_SECRET需要提供)
# VALUE_CANVAS_API_TOKEN=your-token-here
